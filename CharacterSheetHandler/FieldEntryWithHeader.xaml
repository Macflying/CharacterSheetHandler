<UserControl x:Class="CharacterSheetHandler.FieldEntryWithHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Name="FieldEntryWithHeaderName">
    <UserControl.Resources>
        <Style TargetType="Line" BasedOn="{StaticResource BlankLine}" />
        <Style TargetType="TextBlock" BasedOn="{StaticResource EntryHeaderTextBlock}" />
    </UserControl.Resources>
    <DockPanel>
        <TextBlock DockPanel.Dock="Left"
                       Text="{Binding ElementName=FieldEntryWithHeaderName, Path=Header}" />
        <Line DockPanel.Dock="Left" />
        <Line DockPanel.Dock="Right" />
        <TextBox Text="{Binding ElementName=FieldEntryWithHeaderName, Path=Text}" />
    </DockPanel>
</UserControl>